const serverErrorClient = { code: 500100, message: 'Internal Error Client' }
const clientErrorClient = { code: 400100, message: 'Check client_id and client_secret' }

module.exports = {
  serverErrorClient,
  clientErrorClient
}
