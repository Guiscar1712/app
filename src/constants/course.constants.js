const MODALITY_OPTIONS = {
  PRESENCIAL: 'PRESENCIAL',
  A_DISTANCIA: 'A DISTÂNCIA',
  EAD: 'EAD',
  SEMIPRESENCIAL: 'SEMIPRESENCIAL',
}

const COURSE_TYPES = {
  TECHNICAL_COURSE: 'CURSO_TECNICO',
  GRADUATION: 'GRADUACAO',
}

const CLASSROOM_COURSES = [MODALITY_OPTIONS.PRESENCIAL]

const DISTANCE_COURSES = [MODALITY_OPTIONS.A_DISTANCIA, MODALITY_OPTIONS.EAD]

const BLENDED_COURSES = [MODALITY_OPTIONS.SEMIPRESENCIAL]

module.exports = {
  COURSE_TYPES,
  CLASSROOM_COURSES,
  DISTANCE_COURSES,
  BLENDED_COURSES,
}
