const enrollments = require('./enrollments.controller')
const notification = require('./notification.controller')
const course = require('./course.controller')
const coursePreviewLead = require('./coursePreviewLead.controller')

module.exports = {
  enrollments,
  notification,
  course,
  coursePreviewLead,
}
